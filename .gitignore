# Harness run outputs (regenerable)
harness/runs/

# Large dataset (64 MB) — re-download on the target machine, see harness/README.md
harness/data/primevul/

# Secrets — never commit real keys (.env.example is the committed template)
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db

# LaTeX build artifacts
*.aux
*.log
*.out
*.bbl
*.blg
*.synctex.gz
*.fls
*.fdb_latexmk
*.toc

# Submission bundles: regenerable via harness/scripts/build_submission_package.py
submission_MDPI/

# Zenodo deposit bundle: regenerable via harness/scripts/build_submission_package.py
harness.zip
